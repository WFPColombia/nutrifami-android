<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.pma.nutrifami">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.StartActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".activity.ModulesActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LectureActivity"
            android:label="@string/module_title" />
        <activity
            android:name=".activity.ProfileActivity"
            android:label="@string/profile_title" />
        <activity android:name=".activity.game.QuizActivity" />
        <activity android:name=".activity.game.PictureQuizActivity" />
        <activity android:name=".activity.game.SwipeActivity" />
        <activity android:name=".activity.game.PairsActivity" />
    </application>

</manifest>